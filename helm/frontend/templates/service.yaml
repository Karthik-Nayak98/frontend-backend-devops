apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.name }}-service"
spec:
  type: NodePort
  selector:
    app: {{ .Values.name }}
  ports:
    - port: 80
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
